---
description: "design_word — Word 생성 (python-docx + Mermaid + PDF)"
---

# /design_word — Word 생성 허브

## 포함 커맨드
- `/make` — Word 파일 생성 (**design_excel과 이름 중복**)
- `/status` — 생성 상태 확인

## 참고
`/make`, `/status`는 design_excel과 이름이 같음. 실제 실행되는 플러그인은 `.claude/commands/`에 남은 파일에 달림.

## 기본 실행
`/make` — .docx 생성 (다이어그램 포함 시 PDF 변환 가능).
