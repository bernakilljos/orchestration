---
description: "예시 커맨드 — 이걸 복사해서 내 커맨드 만들기"
allowed-tools: Bash(where:*), Bash(powershell:*)
---

## Context
(필요하면 !`명령어` 로 실시간 값 주입)

## Your task

여기에 Claude가 실행할 내용을 한국어로 세세하게 작성한다.

예시:
1. 파일 A를 읽고
2. B 조건이면 X 실행
3. 결과를 표로 출력

---
> 이 파일을 복사해서 commands/ 폴더에 저장하면 /example 커맨드로 실행됨.
> 파일명 = 커맨드명. my-report.md → /my-report
